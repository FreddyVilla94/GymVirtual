<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.villa.gymvirtual">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".GUI.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GUI.RegisterActivity">
            
        </activity>
        <activity android:name=".GUI.UserActivity">

        </activity>
        <activity android:name=".GUI.Exercise_Activity">

        </activity>
        <activity android:name=".GUI.ExerciseRoutineActivity">

        </activity>
        <activity android:name=".GUI.NotificationActivity">

        </activity>
    </application>

</manifest>